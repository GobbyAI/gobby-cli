{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://gobby.ai/schemas/ghook/diagnose-output.v2.schema.json",
  "title": "Gobby ghook --diagnose output",
  "description": "Output of `ghook --diagnose --cli=<c> --type=<t>`. Schema version 2 — adds install_method and install_source_url for install-provenance reporting. v1 fields are unchanged.",
  "type": "object",
  "required": [
    "schema_version",
    "ghook_version",
    "cli",
    "hook_type",
    "critical",
    "terminal_context_enabled",
    "daemon_url",
    "daemon_host",
    "daemon_port",
    "cli_recognized"
  ],
  "additionalProperties": false,
  "properties": {
    "schema_version": {
      "type": "integer",
      "const": 2
    },
    "ghook_version": {
      "type": "string",
      "minLength": 1
    },
    "cli": {
      "type": "string",
      "minLength": 1
    },
    "hook_type": {
      "type": "string",
      "minLength": 1
    },
    "source": {
      "type": ["string", "null"]
    },
    "critical": {
      "type": "boolean"
    },
    "terminal_context_enabled": {
      "type": "boolean"
    },
    "daemon_url": {
      "type": "string",
      "minLength": 1
    },
    "daemon_host": {
      "type": "string",
      "minLength": 1
    },
    "daemon_port": {
      "type": "integer",
      "minimum": 1,
      "maximum": 65535
    },
    "project_root": {
      "type": ["string", "null"]
    },
    "project_id": {
      "type": ["string", "null"]
    },
    "terminal_context_preview": {
      "type": ["object", "null"]
    },
    "cli_recognized": {
      "type": "boolean"
    },
    "install_method": {
      "type": ["string", "null"],
      "description": "How ghook was installed, sourced from the install sidecar (.ghook-install.json) next to the binary. Conventional values: github-release, crates-binstall, cargo-install, manual, unknown. null when no sidecar is present (e.g. cargo install without a sidecar-writing installer)."
    },
    "install_source_url": {
      "type": ["string", "null"],
      "description": "URL the binary was fetched from (typically a GitHub release asset URL), sourced from the install sidecar. null when unknown or not applicable."
    },
    "failure_dir": {
      "type": "string",
      "description": "Directory where ghook writes bounded local failure diagnostics."
    },
    "recent_failure_count": {
      "type": "integer",
      "minimum": 0,
      "description": "Total number of recent failure artifact files found under failure_dir."
    },
    "recent_failures": {
      "type": "array",
      "description": "Recent ghook failure artifact metadata, newest first.",
      "items": {
        "type": "object",
        "required": ["path", "modified_at_unix_ms"],
        "additionalProperties": false,
        "properties": {
          "path": {
            "type": "string"
          },
          "modified_at_unix_ms": {
            "type": ["integer", "null"],
            "minimum": 0
          }
        }
      }
    }
  }
}
