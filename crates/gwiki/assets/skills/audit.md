# Audit Workflow

Use this prompt when checking wiki pages for unsupported claims.

- Compare page claims against provenance and inline citations.
- Report unsupported claims with page paths and line numbers.
- Include source context that can resolve each claim.
- Write audit reports under `outputs/`.
