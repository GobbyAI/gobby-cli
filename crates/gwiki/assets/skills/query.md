# Query Workflow

Use this prompt when answering questions from a gwiki scope.

- Answer from indexed wiki pages and raw sources.
- Cite both the source path and the wiki page used.
- Surface degraded search paths when a backend is unavailable.
- Keep query exports under `outputs/`.
