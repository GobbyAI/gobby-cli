# Wiki bake-off — STATUS / resume breadcrumb (2026-06-14)

Durable resume note (survives compaction). Plan: `/Users/josh/Projects/gobby-cli/.gobby/plans/wiki-fix.md` WP4. Gobby task **#734** (claimed — re-claim via `claim_task #734` if an edit hook blocks file writes). Two tracks (see `ADOPTION-CANDIDATES.md` header).

## Per-tool status
| Tool | Track | Status | Artifacts |
|---|---|---|---|
| DeepWiki-Open | A code→docs | ✅ DONE, scored | `outputs/deepwiki-open/` (SCORECARD.md, wiki_combined.md, 10 pages) |
| CodeWiki (Rust / gobby-cli) | A | ✅ DONE — honest FAIL (Rust unsupported even at HEAD `06e6d01`; validator gate excludes `.rs`) | `outputs/CodeWiki/SCORECARD.md` |
| CodeWiki (Python / `src/gobby/agents`, 65 files) | A2 | ✅ DONE — concluded. 3 attempts, no complete wiki under local gemma (600s timeout; 1800s-patched got good 6-module semantic clustering + 1 module-overview w/ diagram, then HUNG on a sub-module). Strengths captured (C5 semantic clustering); grounding = symbol-name only (no file:line). `outputs/CodeWiki-python/SCORECARD.md` |
| gobby codewiki (same Python scope) | A2 | ✅ DONE — 65 file + 3 module docs, 630 symbols w/ per-symbol API, dense resolvable file:line citations; 0 diagrams (graph-truncated → C4). `outputs/gobby-codewiki-agents/` |
| **Graphify** | **A1 code-graph (↔ gcode)** | ✅ **DONE, scored** — SUCCESS on Rust. Built 10,842-node/25,859-edge/403-community graph (local AST, no LLM for code graph). Q1–Q4 battery run vs gcode: **parity** on file:line call/impact traversal; **gcode wins** overall (semantic ranking + per-symbol purpose + real source retrieval + hybrid search); **Graphify wins** 3 adoptable features → **C6** path-between-concepts, **C7** per-edge confidence audit, **C8** token-budget output; **C5** Leiden communities confirmed cross-tool. `--cargo` crashed on subtree scope (logged). `outputs/graphify/` (SCORECARD.md, SETUP.md, graph.json). Community-naming pass running (append to SCORECARD). | `outputs/graphify/` |
| **OpenDeepWiki** | **A2 code→docs** | ✅ **DONE, scored** — SUCCESS on Rust (backend-only build; LM Studio gemma). Full 13-node hierarchical wiki (~23.7min), all nodes persisted (4 WriteDoc-miss self-healed on retry). **19 mermaid diagrams** across 11/13 docs; precise line-anchored citations (`#L9-L15`) but only in 4 docs; **bimodal depth** — ~8 strong docs + flagship `gcode-search` a 20L STUB (gemma WriteDoc function-call miss, a local-model artifact). gobby codewiki wins completeness/uniform-depth/citation-density; OpenDeepWiki wins diagrams (**C4 confirmed**) + hierarchical catalog (**C1 confirmed, new C9**). Config gotcha (flat `WIKI_*_MODEL` env didn't bind; fixed via admin API) logged. Stack torn down. `outputs/opendeepwiki/` (SCORECARD.md, SETUP.md, wiki/, wiki-json/) | `outputs/opendeepwiki/` |
| **llm-wiki** | **B sources→vault (↔ gwiki)** | ✅ **DONE, scored** — **GAP ANALYSIS, not a contest.** llm-wiki = vertical specialist (AI coding-session `.jsonl` → Karpathy vault); gwiki = generalist with **no session feature**. Same 8 native Claude Code sessions (this program) → both. llm-wiki: 8 full per-session reconstructions w/ model/tool/token/duration metadata + redaction + 8 gemma4:31b synth source pages (accurate, `[[wikilinks]]`) + `llms.txt`/JSON-LD exports + 16-rule lint. gwiki: 8 **opaque 525B stubs** (0 extraction), `compile` **#733-blocked on fresh vault** (reproduced). Model-robust synth note: single-shot per-page → clean gemma output where agent-loop tools failed. → **C10** (session-transcript pipeline; gobby already archives the raw material at `~/.gobby/session_transcripts/`). Honest other direction: gwiki owns multimodal research (URLs/PDFs/AV) llm-wiki can't touch. Synth via Ollama gemma4:31b (model delta vs 26b-a4b-qat baseline noted). `outputs/llm-wiki/` + `outputs/gwiki-track-b/` | `outputs/llm-wiki/` |

## ON RESUME — WP4 + WP5 COMPLETE
DeepWiki ✅ · CodeWiki(Rust) ✅ · CodeWiki(Py) ✅ · gobby-codewiki ✅ · Graphify ✅ · OpenDeepWiki ✅ · **llm-wiki ✅**. Adoption candidates **C1–C10** + W1 logged in `ADOPTION-CANDIDATES.md`.
**WP5 DONE (2026-06-14):** `fable-repo-analysis.md` → "Phase 4 — Live parity-plus proof" appended (3 per-track matrices + adoption analysis); adoption epic **#761** + leaf tasks **#762–#771** (one per C1–C10); lightweight evidence committed to `docs/evidence/wiki-bakeoff-2026-06/`. **Next = final self-audit**, then `[gobby-cli-#734]` commit (in progress) → Josh reviews/pushes.

## WP5 deliverable (after all runs)
Append "Live parity+ proof (2026-06)" to `/Users/josh/Projects/gobby-cli/fable-repo-analysis.md`: two-track matrix re-scored, honest verdicts incl. competitor wins, + **"What competitors do better → what to adopt"** deep analysis sourced from `ADOPTION-CANDIDATES.md`. Then create **one gobby-cli epic with one leaf task per adoption candidate** (deduped across tools). Commit WP4 work `[gobby-cli-#734] ...` (includes wiki-fix.md WP4/WP5 edits). Josh pushes/tags — never Claude.

## Environment (durable facts)
- LM Studio host :1234, OpenAI-compatible, token-auth REQUIRED. Key = `OPENAI_API_KEY` in `/Users/josh/Projects/gobby/.env` (mode 600, value `sk-…` 35 chars). NEVER print/commit. `~/Projects/wiki-bakeoff/.env` mirrors it (populated).
- Chat model `google/gemma-4-26b-a4b-qat` (REASONING model — emits `reasoning_content`; with adequate budget `content` populates fine; use each tool's OWN defaults, no token tuning). Embeddings `text-embedding-nomic-embed-text-v1.5` (768-dim).
- Container → host: `http://host.docker.internal:1234/v1` (compose needs `extra_hosts: host.docker.internal:host-gateway`). Host/local: `http://localhost:1234/v1`.
- gobby Python repo `/Users/josh/Projects/gobby` IS gcode-indexed (4024 files, 55923 symbols). gobby-cli has 209GB `target/` — always feed tools a `git archive HEAD` clean copy.
- Daemon containers (DO NOT TOUCH): `gobby-postgres`, `gobby-postgres-test-1`, `services-falkordb-1`, `services-qdrant-1`. Images left on disk: `deepwiki-open-deepwiki:latest` (1.2GB), `codewiki:0.0.1` (3.26GB).
- Constraints: serialize Docker stacks (daemon stack already up — don't stack 5 more); no `git push`/`git add -A`/secrets; `GOBBY_TEST_PROTECT=1` on pytest; competitor clones in `~/Projects/wiki-bakeoff/<tool>/`.
- **PRESERVE outputs (Josh, 2026-06-14):** do NOT delete `outputs/` or `inputs/` and do NOT commit them — they are the BEFORE-baseline for a planned **re-run of the battery after gobby implements adoption candidates C1–C9**. Cleanup = tear down containers only; competitor images (~5.7GB) are rebuildable but leave them to save re-run time. (memory `12bc03d0`)
