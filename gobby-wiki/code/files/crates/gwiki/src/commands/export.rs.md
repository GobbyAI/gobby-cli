---
title: crates/gwiki/src/commands/export.rs
type: code_file
provenance:
- file: crates/gwiki/src/commands/export.rs
generated_by: gcode-codewiki
trust: generated
freshness: indexed
---

# crates/gwiki/src/commands/export.rs

Module: [[code/modules/crates/gwiki/src/commands|crates/gwiki/src/commands]]

## Overview

`crates/gwiki/src/commands/export.rs` exposes 1 indexed API symbol.

## How it fits

`crates/gwiki/src/commands/export.rs` is documented from its indexed symbols; see the Reference table below and the module page for how it connects to sibling files.

## Reference

| Symbol | Kind | Purpose |
| --- | --- | --- |
| `execute` | function | Resolves the selected scope, runs the export command at that scope root, serializes the resulting 'ExportOutput' to JSON, and returns a scoped 'CommandOutcome' whose human-readable text lists the exported artifact paths. [crates/gwiki/src/commands/export.rs:4-30] |

